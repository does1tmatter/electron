const defaultNoteWidth = 380
const defaultNoteHeight = 450

const loaderWidth = 400
const loaderHeight = 600

const notesDir = '/notes'

// prettier-ignore
export {
  defaultNoteWidth,
  defaultNoteHeight,
  loaderWidth,
  loaderHeight,
  notesDir
}
